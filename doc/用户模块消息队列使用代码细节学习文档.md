# 用户模块消息队列使用代码细节学习文档

## 一、概述

本文档基于 FNUSALE 项目实际代码，详细讲解用户模块中消息队列（RocketMQ）的使用方式和代码实现细节。

### 1.1 技术选型

| 技术组件 | 版本 | 说明 |
|---------|------|------|
| Apache RocketMQ | 2.2.3 | 消息中间件 |
| Spring Boot | 3.x | 应用框架 |
| Redis | - | 幂等性支持 |

### 1.2 核心架构

```
用户注册请求
    ↓
UserServiceImpl / CaptchaServiceImpl
    ↓
UserRegisterEventPublisher.publishRegisterEvent()
    ↓
┌─────────────────────────────────────────────────────────────┐
│              RocketMQ Topic: user-register                   │
├─────────────────────────────────────────────────────────────┤
│ Tag: init-points     → UserRegisterPointsConsumer (用户模块) │
│ Tag: welcome         → UserRegisterWelcomeConsumer (用户模块)│
│ Tag: new-user-coupon → UserRegisterCouponConsumer (营销模块) │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、核心文件清单

### 2.1 消息生产者

| 文件路径 | 说明 |
|---------|------|
| [UserRegisterEventPublisher.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/service/UserRegisterEventPublisher.java) | 用户注册事件发布接口 |
| [UserRegisterEventPublisherImpl.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/service/impl/UserRegisterEventPublisherImpl.java) | 用户注册事件发布实现 |

### 2.2 消息消费者

| 文件路径 | 所属模块 | 说明 |
|---------|---------|------|
| [UserRegisterPointsConsumer.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/mq/consumer/UserRegisterPointsConsumer.java) | 用户模块 | 积分初始化消费者 |
| [UserRegisterWelcomeConsumer.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/mq/consumer/UserRegisterWelcomeConsumer.java) | 用户模块 | 欢迎通知消费者 |
| [UserRegisterCouponConsumer.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-marketing/src/main/java/com/fnusale/marketing/mq/consumer/UserRegisterCouponConsumer.java) | 营销模块 | 新人优惠券消费者 |

### 2.3 事件定义与常量

| 文件路径 | 说明 |
|---------|------|
| [UserRegisterEvent.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-common/src/main/java/com/fnusale/common/event/UserRegisterEvent.java) | 用户注册事件对象 |
| [RocketMQConstants.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-common/src/main/java/com/fnusale/common/constant/RocketMQConstants.java) | RocketMQ Topic/Tag 常量定义 |

---

## 三、Topic/Tag 设计详解

### 3.1 常量定义

文件：[RocketMQConstants.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-common/src/main/java/com/fnusale/common/constant/RocketMQConstants.java)

```java
public class RocketMQConstants {

    private RocketMQConstants() {
    }

    // ==================== 用户相关 Topic ====================

    /**
     * 用户注册 Topic
     */
    public static final String USER_REGISTER_TOPIC = "user-register";

    /**
     * 用户注册 - 初始化积分 Tag
     */
    public static final String USER_REGISTER_TAG_INIT_POINTS = "init-points";

    /**
     * 用户注册 - 发送欢迎通知 Tag
     */
    public static final String USER_REGISTER_TAG_WELCOME = "welcome";

    /**
     * 用户注册 - 发放新人优惠券 Tag
     */
    public static final String USER_REGISTER_TAG_NEW_USER_COUPON = "new-user-coupon";
    
    // ... 其他 Topic 定义
}
```

### 3.2 Topic-Tag 设计理念

| Topic | Tag | 消费者组 | 业务含义 |
|-------|-----|---------|---------|
| user-register | init-points | user-register-points-group | 初始化用户积分 |
| user-register | welcome | user-register-welcome-group | 发送欢迎通知 |
| user-register | new-user-coupon | user-register-coupon-group | 发放新人优惠券 |

**设计优势**：
- 同一 Topic 下通过 Tag 区分不同业务场景
- 消费者组隔离，互不影响
- 支持消息过滤，消费者只订阅感兴趣的 Tag

---

## 四、事件对象详解

### 4.1 UserRegisterEvent 事件类

文件：[UserRegisterEvent.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-common/src/main/java/com/fnusale/common/event/UserRegisterEvent.java)

```java
/**
 * 用户注册事件
 * 用于异步处理注册后置操作：积分初始化、欢迎通知、新人优惠券等
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
public class UserRegisterEvent {

    /**
     * 用户ID
     */
    private Long userId;

    /**
     * 用户名
     */
    private String username;

    /**
     * 手机号（可能为空）
     */
    private String phone;

    /**
     * 邮箱（可能为空）
     */
    private String email;

    /**
     * 身份类型（STUDENT/TEACHER）
     */
    private String identityType;

    /**
     * 注册来源（PHONE/EMAIL/CAPTCHA_LOGIN）
     */
    private String registerSource;

    /**
     * 注册时间
     */
    private LocalDateTime registerTime;

    /**
     * 事件ID（用于幂等性处理）
     */
    private String eventId;
}
```

### 4.2 字段设计说明

| 字段 | 类型 | 说明 | 用途 |
|-----|------|------|------|
| userId | Long | 用户ID | 关联用户主体 |
| username | String | 用户名 | 欢迎消息展示 |
| phone | String | 手机号 | 短信通知（可能为空） |
| email | String | 邮箱 | 邮件通知（可能为空） |
| identityType | String | 身份类型 | 业务判断 |
| registerSource | String | 注册来源 | 数据统计 |
| registerTime | LocalDateTime | 注册时间 | 业务记录 |
| eventId | String | 事件ID | **幂等性保证** |

---

## 五、消息生产者详解

### 5.1 接口定义

文件：[UserRegisterEventPublisher.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/service/UserRegisterEventPublisher.java)

```java
/**
 * 用户注册事件发布服务
 */
public interface UserRegisterEventPublisher {

    /**
     * 发布用户注册事件
     * @param event 注册事件
     */
    void publishRegisterEvent(UserRegisterEvent event);
}
```

### 5.2 实现类详解

文件：[UserRegisterEventPublisherImpl.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/service/impl/UserRegisterEventPublisherImpl.java)

```java
@Slf4j
@Service
@SuppressWarnings("SpringJavaInjectionPointsAutowiringInspection")
public class UserRegisterEventPublisherImpl implements UserRegisterEventPublisher {

    private RocketMQTemplate rocketMQTemplate;

    /**
     * 可选注入：支持无 MQ 环境运行
     * 当 RocketMQ 未配置时，rocketMQTemplate 为 null
     */
    @Autowired(required = false)
    public void setRocketMQTemplate(RocketMQTemplate rocketMQTemplate) {
        this.rocketMQTemplate = rocketMQTemplate;
    }

    @Override
    public void publishRegisterEvent(UserRegisterEvent event) {
        // 1. 空值检查：支持无 MQ 环境
        if (rocketMQTemplate == null) {
            log.warn("RocketMQTemplate未配置，跳过用户注册事件发布, userId: {}", event.getUserId());
            return;
        }

        // 2. 发送初始化积分消息
        sendAsyncMessage(
                RocketMQConstants.USER_REGISTER_TOPIC + ":" + RocketMQConstants.USER_REGISTER_TAG_INIT_POINTS,
                event
        );

        // 3. 发送欢迎通知消息
        sendAsyncMessage(
                RocketMQConstants.USER_REGISTER_TOPIC + ":" + RocketMQConstants.USER_REGISTER_TAG_WELCOME,
                event
        );

        // 4. 发送新人优惠券消息
        sendAsyncMessage(
                RocketMQConstants.USER_REGISTER_TOPIC + ":" + RocketMQConstants.USER_REGISTER_TAG_NEW_USER_COUPON,
                event
        );

        log.info("用户注册事件已发布, userId: {}, eventId: {}", event.getUserId(), event.getEventId());
    }

    /**
     * 异步发送消息
     * @param destination 目标地址，格式：topic:tag
     * @param event 消息内容
     */
    private void sendAsyncMessage(String destination, UserRegisterEvent event) {
        rocketMQTemplate.asyncSend(destination, event, new SendCallback() {
            @Override
            public void onSuccess(SendResult sendResult) {
                log.debug("消息发送成功, destination: {}, msgId: {}", destination, sendResult.getMsgId());
            }

            @Override
            public void onException(Throwable e) {
                log.error("消息发送失败, destination: {}, userId: {}", destination, event.getUserId(), e);
            }
        });
    }
}
```

### 5.3 关键设计点解析

#### 5.3.1 可选注入设计

```java
@Autowired(required = false)
public void setRocketMQTemplate(RocketMQTemplate rocketMQTemplate) {
    this.rocketMQTemplate = rocketMQTemplate;
}
```

**设计目的**：
- 支持开发环境无 MQ 运行
- 避免因 MQ 未配置导致启动失败
- 提供优雅降级能力

#### 5.3.2 异步发送设计

```java
rocketMQTemplate.asyncSend(destination, event, new SendCallback() {
    @Override
    public void onSuccess(SendResult sendResult) {
        // 成功回调
    }

    @Override
    public void onException(Throwable e) {
        // 失败回调
    }
});
```

**设计优势**：
- 不阻塞主业务流程
- 提高系统吞吐量
- 通过回调处理发送结果

#### 5.3.3 Topic:Tag 格式

```java
RocketMQConstants.USER_REGISTER_TOPIC + ":" + RocketMQConstants.USER_REGISTER_TAG_INIT_POINTS
// 等价于 "user-register:init-points"
```

---

## 六、消息消费者详解

### 6.1 积分初始化消费者

文件：[UserRegisterPointsConsumer.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/mq/consumer/UserRegisterPointsConsumer.java)

```java
/**
 * 用户注册事件消费者 - 初始化积分
 * 注意：积分初始化已在注册流程中同步完成，此消费者作为兜底和幂等性检查
 */
@Slf4j
@Component
@RocketMQMessageListener(
        topic = RocketMQConstants.USER_REGISTER_TOPIC,
        consumerGroup = "user-register-points-group",
        selectorExpression = RocketMQConstants.USER_REGISTER_TAG_INIT_POINTS
)
@RequiredArgsConstructor
@ConditionalOnProperty(name = "rocketmq.name-server")
public class UserRegisterPointsConsumer implements RocketMQListener<UserRegisterEvent> {

    private final StringRedisTemplate redisTemplate;

    /**
     * 积分初始化幂等性 Key 前缀
     */
    private static final String POINTS_INIT_KEY_PREFIX = "user:points:init:";

    @Override
    public void onMessage(UserRegisterEvent event) {
        Long userId = event.getUserId();
        String eventId = event.getEventId();

        log.info("收到用户注册积分初始化消息, userId: {}, eventId: {}", userId, eventId);

        // 幂等性检查：使用 Redis SETNX
        String idempotentKey = POINTS_INIT_KEY_PREFIX + eventId;
        Boolean success = redisTemplate.opsForValue().setIfAbsent(idempotentKey, "1", 24, TimeUnit.HOURS);
        if (Boolean.FALSE.equals(success)) {
            log.info("积分初始化已处理，跳过, userId: {}, eventId: {}", userId, eventId);
            return;
        }

        try {
            // 积分已在注册流程中初始化，此处仅作为兜底检查
            // 实际场景中可以调用积分服务进行校验和补偿初始化
            log.info("用户积分初始化完成（已由注册流程处理）, userId: {}", userId);
        } catch (Exception e) {
            // 处理失败，删除幂等性 Key，允许重试
            redisTemplate.delete(idempotentKey);
            log.error("用户积分初始化失败, userId: {}", userId, e);
            throw e;  // 抛出异常，触发消息重试
        }
    }
}
```

### 6.2 欢迎通知消费者

文件：[UserRegisterWelcomeConsumer.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/mq/consumer/UserRegisterWelcomeConsumer.java)

```java
/**
 * 用户注册事件消费者 - 发送欢迎通知
 */
@Slf4j
@Component
@RocketMQMessageListener(
        topic = RocketMQConstants.USER_REGISTER_TOPIC,
        consumerGroup = "user-register-welcome-group",
        selectorExpression = RocketMQConstants.USER_REGISTER_TAG_WELCOME
)
@RequiredArgsConstructor
@ConditionalOnProperty(name = "rocketmq.name-server")
public class UserRegisterWelcomeConsumer implements RocketMQListener<UserRegisterEvent> {

    private final StringRedisTemplate redisTemplate;
    private final EmailService emailService;

    /**
     * 欢迎消息幂等性 Key 前缀
     */
    private static final String WELCOME_KEY_PREFIX = "user:welcome:";

    @Override
    public void onMessage(UserRegisterEvent event) {
        Long userId = event.getUserId();
        String eventId = event.getEventId();

        log.info("收到用户注册欢迎通知消息, userId: {}, eventId: {}", userId, eventId);

        // 幂等性检查
        String idempotentKey = WELCOME_KEY_PREFIX + eventId;
        Boolean success = redisTemplate.opsForValue().setIfAbsent(idempotentKey, "1", 24, TimeUnit.HOURS);
        if (Boolean.FALSE.equals(success)) {
            log.info("欢迎通知已发送，跳过, userId: {}, eventId: {}", userId, eventId);
            return;
        }

        try {
            // 发送欢迎通知
            sendWelcomeNotification(event);
            log.info("欢迎通知发送成功, userId: {}", userId);
        } catch (Exception e) {
            // 处理失败，删除幂等性 Key，允许重试
            redisTemplate.delete(idempotentKey);
            log.error("欢迎通知发送失败, userId: {}", userId, e);
            throw e;
        }
    }

    /**
     * 发送欢迎通知
     */
    private void sendWelcomeNotification(UserRegisterEvent event) {
        // 发送欢迎邮件
        if (event.getEmail() != null && !event.getEmail().isEmpty()) {
            try {
                emailService.sendWelcomeEmail(event.getEmail(), event.getUsername());
                log.info("欢迎邮件发送成功 - email: {}", DesensitizeUtil.email(event.getEmail()));
            } catch (Exception e) {
                log.error("欢迎邮件发送失败 - email: {}, 错误: {}", 
                    DesensitizeUtil.email(event.getEmail()), e.getMessage(), e);
            }
        }

        // TODO: 后续集成短信服务
        if (event.getPhone() != null && !event.getPhone().isEmpty()) {
            log.info("欢迎短信发送（待集成） - phone: {}", DesensitizeUtil.phone(event.getPhone()));
        }

        // TODO: 后续集成 IM 服务
        log.info("欢迎站内消息发送（待集成） - userId: {}", event.getUserId());
    }
}
```

### 6.3 新人优惠券消费者（营销模块）

文件：[UserRegisterCouponConsumer.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-marketing/src/main/java/com/fnusale/marketing/mq/consumer/UserRegisterCouponConsumer.java)

```java
/**
 * 用户注册事件消费者 - 发放新人优惠券
 */
@Slf4j
@Component
@RocketMQMessageListener(
        topic = RocketMQConstants.USER_REGISTER_TOPIC,
        consumerGroup = "user-register-coupon-group",
        selectorExpression = RocketMQConstants.USER_REGISTER_TAG_NEW_USER_COUPON
)
@RequiredArgsConstructor
public class UserRegisterCouponConsumer implements RocketMQListener<UserRegisterEvent> {

    private final UserCouponMapper userCouponMapper;
    private final CouponMapper couponMapper;
    private final StringRedisTemplate redisTemplate;

    /**
     * 新人优惠券发放幂等性 Key 前缀
     */
    private static final String NEW_USER_COUPON_KEY_PREFIX = "user:coupon:new:";

    /**
     * 新人优惠券 ID
     */
    private static final Long NEW_USER_COUPON_ID = 1L;

    /**
     * 优惠券有效期（天）
     */
    private static final int COUPON_VALID_DAYS = 30;

    @Override
    public void onMessage(UserRegisterEvent event) {
        Long userId = event.getUserId();
        String eventId = event.getEventId();

        log.info("收到用户注册新人优惠券消息, userId: {}, eventId: {}", userId, eventId);

        // 幂等性检查
        String idempotentKey = NEW_USER_COUPON_KEY_PREFIX + eventId;
        Boolean success = redisTemplate.opsForValue().setIfAbsent(idempotentKey, "1", 24, TimeUnit.HOURS);
        if (Boolean.FALSE.equals(success)) {
            log.info("新人优惠券已发放，跳过, userId: {}, eventId: {}", userId, eventId);
            return;
        }

        try {
            // 检查优惠券是否存在
            if (couponMapper.selectById(NEW_USER_COUPON_ID) == null) {
                log.warn("新人优惠券不存在, couponId: {}", NEW_USER_COUPON_ID);
                return;
            }

            // 检查用户是否已领取（双重保障）
            if (userCouponMapper.countByUserAndCoupon(userId, NEW_USER_COUPON_ID) > 0) {
                log.info("用户已领取新人优惠券, userId: {}", userId);
                return;
            }

            // 发放新人优惠券
            UserCoupon userCoupon = UserCoupon.builder()
                    .userId(userId)
                    .couponId(NEW_USER_COUPON_ID)
                    .receiveTime(LocalDateTime.now())
                    .expireTime(LocalDateTime.now().plusDays(COUPON_VALID_DAYS))
                    .couponStatus("UNUSED")
                    .createTime(LocalDateTime.now())
                    .build();

            userCouponMapper.insert(userCoupon);

            log.info("新人优惠券发放成功, userId: {}, couponId: {}", userId, NEW_USER_COUPON_ID);
        } catch (Exception e) {
            // 处理失败，删除幂等性 Key，允许重试
            redisTemplate.delete(idempotentKey);
            log.error("新人优惠券发放失败, userId: {}", userId, e);
            throw e;
        }
    }
}
```

---

## 七、消费者注解详解

### 7.1 @RocketMQMessageListener 注解

```java
@RocketMQMessageListener(
        topic = RocketMQConstants.USER_REGISTER_TOPIC,        // 订阅的 Topic
        consumerGroup = "user-register-points-group",          // 消费者组名
        selectorExpression = RocketMQConstants.USER_REGISTER_TAG_INIT_POINTS  // Tag 过滤表达式
)
```

| 属性 | 说明 | 示例 |
|-----|------|------|
| topic | 订阅的消息主题 | "user-register" |
| consumerGroup | 消费者组名，同组内消费者负载均衡 | "user-register-points-group" |
| selectorExpression | 消息过滤表达式，支持 Tag 过滤 | "init-points" |

### 7.2 @ConditionalOnProperty 注解

```java
@ConditionalOnProperty(name = "rocketmq.name-server")
```

**作用**：
- 只有当配置了 `rocketmq.name-server` 时才加载该消费者
- 支持开发环境无 MQ 运行
- 避免因 MQ 未配置导致启动失败

---

## 八、幂等性设计详解

### 8.1 幂等性实现方案

```java
// 1. 构建幂等性 Key
String idempotentKey = PREFIX + eventId;

// 2. 使用 Redis SETNX 实现幂等性检查
Boolean success = redisTemplate.opsForValue().setIfAbsent(idempotentKey, "1", 24, TimeUnit.HOURS);

// 3. 判断是否已处理
if (Boolean.FALSE.equals(success)) {
    log.info("消息已处理，跳过");
    return;
}

try {
    // 4. 执行业务逻辑
    doBusiness();
} catch (Exception e) {
    // 5. 失败时删除 Key，允许重试
    redisTemplate.delete(idempotentKey);
    throw e;
}
```

### 8.2 幂等性 Key 设计

| 消费者 | Key 前缀 | 完整 Key 示例 |
|-------|---------|--------------|
| UserRegisterPointsConsumer | user:points:init: | user:points:init:uuid-xxx |
| UserRegisterWelcomeConsumer | user:welcome: | user:welcome:uuid-xxx |
| UserRegisterCouponConsumer | user:coupon:new: | user:coupon:new:uuid-xxx |

### 8.3 幂等性保证流程图

```
消息到达
    ↓
┌─────────────────────────────────┐
│ Redis SETNX(eventId)            │
│ SETNX Key = prefix + eventId    │
└─────────────────────────────────┘
    ↓
  成功？ ──── 否 ──→ 跳过处理（已处理过）
    │
   是
    ↓
┌─────────────────────────────────┐
│ 执行业务逻辑                     │
└─────────────────────────────────┘
    ↓
  成功？ ──── 否 ──→ 删除 Key + 抛异常（允许重试）
    │
   是
    ↓
  完成
```

---

## 九、消息发布调用点

### 9.1 手机号注册发布事件

文件：[UserServiceImpl.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/service/impl/UserServiceImpl.java)

```java
@Override
@Transactional(rollbackFor = Exception.class)
public void registerByPhone(UserRegisterDTO dto) {
    // ... 注册逻辑
    
    userMapper.insert(user);
    initUserPoints(user.getId());
    publishRegisterEvent(user, "PHONE");  // 发布注册事件
    
    log.info("用户注册成功，userId: {}, phone: {}", user.getId(), DesensitizeUtil.phone(dto.getPhone()));
}
```

### 9.2 邮箱注册发布事件

```java
@Override
@Transactional(rollbackFor = Exception.class)
public void registerByEmail(UserRegisterDTO dto) {
    // ... 注册逻辑
    
    userMapper.insert(user);
    initUserPoints(user.getId());
    publishRegisterEvent(user, "EMAIL");  // 发布注册事件
    
    log.info("用户注册成功，userId: {}, email: {}", user.getId(), DesensitizeUtil.email(dto.getEmail()));
}
```

### 9.3 验证码登录自动注册发布事件

文件：[CaptchaServiceImpl.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/java/com/fnusale/user/service/impl/CaptchaServiceImpl.java)

```java
// 验证码登录自动创建用户
user = createNewUser(account);
publishRegisterEvent(user, account);  // 发布注册事件
log.info("验证码登录自动创建用户: {}", account);
```

### 9.4 事件构建方法

```java
private void publishRegisterEvent(User user, String registerSource) {
    UserRegisterEvent event = UserRegisterEvent.builder()
            .userId(user.getId())
            .username(user.getUsername())
            .phone(user.getPhone())
            .email(user.getCampusEmail())
            .identityType(user.getIdentityType())
            .registerSource(registerSource)
            .registerTime(LocalDateTime.now())
            .eventId(UUID.randomUUID().toString())  // 唯一事件ID
            .build();

    eventPublisher.publishRegisterEvent(event);
}
```

---

## 十、RocketMQ 配置详解

### 10.1 application.yml 配置

文件：[application.yml](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/src/main/resources/application.yml)

```yaml
# RocketMQ 配置
rocketmq:
  name-server: ${ROCKETMQ_NAME_SERVER:localhost:9876}  # NameServer 地址
  producer:
    group: user-producer-group           # 生产者组名
    send-message-timeout: 3000           # 发送超时时间（毫秒）
    retry-times-when-send-failed: 2      # 发送失败重试次数
```

### 10.2 配置项说明

| 配置项 | 默认值 | 说明 |
|-------|-------|------|
| name-server | localhost:9876 | RocketMQ NameServer 地址 |
| producer.group | user-producer-group | 生产者组名 |
| producer.send-message-timeout | 3000 | 消息发送超时时间 |
| producer.retry-times-when-send-failed | 2 | 发送失败重试次数 |

---

## 十一、核心设计模式总结

### 11.1 设计模式一览

| 设计模式 | 应用场景 | 代码位置 |
|---------|---------|---------|
| 接口隔离 | 事件发布接口 | UserRegisterEventPublisher |
| 异步消息 | 消息发送 | asyncSend + SendCallback |
| 幂等性模式 | 消息消费 | Redis SETNX |
| 条件装配 | 消费者加载 | @ConditionalOnProperty |
| 建造者模式 | 事件构建 | UserRegisterEvent.builder() |

### 11.2 关键设计特点

| 特性 | 实现方式 | 优势 |
|------|---------|------|
| **多 Tag 消息** | 同一事件发送到多个 Tag | 解耦业务逻辑 |
| **可选注入** | @Autowired(required = false) | 支持无 MQ 环境运行 |
| **异步发送** | asyncSend + 回调 | 不阻塞主流程 |
| **幂等性保证** | Redis SETNX + eventId | 防止重复消费 |
| **兜底机制** | 消费者补偿检查 | 保证数据完整性 |
| **条件装配** | @ConditionalOnProperty | 按需加载消费者 |
| **数据脱敏** | DesensitizeUtil | 保护用户隐私 |

---

## 十二、消息流转完整流程

### 12.1 时序图

```
┌─────────┐    ┌─────────────┐    ┌───────────┐    ┌──────────┐    ┌──────────┐
│ Client  │    │ UserService │    │Publisher  │    │ RocketMQ │    │Consumer  │
└────┬────┘    └──────┬──────┘    └─────┬─────┘    └────┬─────┘    └────┬─────┘
     │                │                 │               │               │
     │  注册请求       │                 │               │               │
     │───────────────>│                 │               │               │
     │                │                 │               │               │
     │                │  创建用户        │               │               │
     │                │────────────────>│               │               │
     │                │                 │               │               │
     │                │  publishEvent   │               │               │
     │                │────────────────>│               │               │
     │                │                 │               │               │
     │                │                 │ asyncSend     │               │
     │                │                 │──────────────>│               │
     │                │                 │               │               │
     │                │                 │               │  推送消息      │
     │                │                 │               │──────────────>│
     │                │                 │               │               │
     │                │                 │               │               │ 幂等检查
     │                │                 │               │               │────┐
     │                │                 │               │               │    │
     │                │                 │               │               │<───┘
     │                │                 │               │               │
     │                │                 │               │               │ 业务处理
     │                │                 │               │               │────┐
     │                │                 │               │               │    │
     │                │                 │               │               │<───┘
     │                │                 │               │               │
     │  注册成功       │                 │               │               │
     │<───────────────│                 │               │               │
     │                │                 │               │               │
```

### 12.2 消息流转步骤

1. **用户发起注册请求**
2. **UserService 处理注册逻辑**
   - 创建用户记录
   - 初始化积分（同步）
3. **发布注册事件**
   - 构建 UserRegisterEvent
   - 调用 eventPublisher.publishRegisterEvent()
4. **异步发送消息**
   - 发送到 init-points Tag
   - 发送到 welcome Tag
   - 发送到 new-user-coupon Tag
5. **消费者处理消息**
   - 幂等性检查
   - 执行业务逻辑
   - 处理失败时删除幂等 Key

---

## 十三、异常处理机制

### 13.1 生产者异常处理

```java
private void sendAsyncMessage(String destination, UserRegisterEvent event) {
    rocketMQTemplate.asyncSend(destination, event, new SendCallback() {
        @Override
        public void onSuccess(SendResult sendResult) {
            log.debug("消息发送成功, destination: {}, msgId: {}", destination, sendResult.getMsgId());
        }

        @Override
        public void onException(Throwable e) {
            // 记录失败日志，不影响主流程
            log.error("消息发送失败, destination: {}, userId: {}", destination, event.getUserId(), e);
        }
    });
}
```

### 13.2 消费者异常处理

```java
try {
    // 执行业务逻辑
    doBusiness();
} catch (Exception e) {
    // 删除幂等性 Key，允许重试
    redisTemplate.delete(idempotentKey);
    log.error("业务处理失败", e);
    throw e;  // 抛出异常，触发 RocketMQ 重试机制
}
```

### 13.3 异常处理策略

| 异常类型 | 处理方式 | 说明 |
|---------|---------|------|
| MQ 未配置 | 跳过发送 | 支持无 MQ 环境运行 |
| 发送失败 | 记录日志 | 不影响主业务流程 |
| 消费失败 | 删除 Key + 重试 | 允许消息重试 |

---

## 十四、最佳实践总结

### 14.1 生产者最佳实践

1. **使用异步发送**：提高系统吞吐量
2. **可选注入**：支持无 MQ 环境运行
3. **合理设置超时**：避免阻塞主流程
4. **日志记录**：便于问题排查

### 14.2 消费者最佳实践

1. **幂等性保证**：使用 Redis SETNX
2. **条件装配**：按需加载消费者
3. **异常处理**：失败时删除幂等 Key
4. **日志脱敏**：保护用户隐私

### 14.3 Topic/Tag 设计最佳实践

1. **Topic 按业务域划分**：user-register、seckill-order 等
2. **Tag 按操作类型划分**：init-points、welcome 等
3. **消费者组隔离**：不同消费者使用不同的组名
4. **常量管理**：统一在 RocketMQConstants 中定义

---

## 十五、相关文档链接

| 文档 | 路径 |
|------|------|
| 用户模块消息队列使用文档v1 | [用户模块消息队列使用文档v1.md](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-user/用户模块消息队列使用文档v1.md) |
| RocketMQ 常量定义 | [RocketMQConstants.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-common/src/main/java/com/fnusale/common/constant/RocketMQConstants.java) |
| 用户注册事件 | [UserRegisterEvent.java](file:///Users/rongx/Desktop/Code/git/FNUSALE/backend/fnusale-common/src/main/java/com/fnusale/common/event/UserRegisterEvent.java) |

---

## 十六、总结

用户模块的消息队列实现采用了 **Apache RocketMQ** 作为消息中间件，通过 **Topic-Tag 模式** 实现消息分类和过滤。核心设计包括：

1. **异步解耦**：用户注册后异步处理积分初始化、欢迎通知、优惠券发放等操作
2. **幂等性保证**：使用 Redis SETNX 实现消息幂等性，防止重复消费
3. **高可用设计**：支持无 MQ 环境运行，消息发送失败不影响主流程
4. **跨服务通信**：营销模块消费用户注册事件，实现服务间解耦
5. **条件装配**：通过 @ConditionalOnProperty 实现按需加载消费者
